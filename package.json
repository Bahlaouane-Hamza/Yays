{
  "name": "youtube-music-player",
  "description": "Collect & save your favorite youtube music links in one place",
  "version": "0.0.1",
  "main": "main.js",
  "dependencies": {
    "express": "^4.13.1",
    "locallydb": "0.0.9",
    "node-notifier": "^4.2.3"
  },
  "scripts": {
    "build": "electron-packager . YoutubePlayer --platform=darwin --arch=x64 --version=0.29.2 --ignore=node_modules/electron --icon=app/images/Icon.icns",
    "start": "electron ."
  },
  "devDependencies": {
    "electron-packager": "^5.0.1"
  }
}
